<?php

class RomanClass extends Model
{
    public function roman_edit_model(): void
    {
        if (
            isset($book["title"]) &&
            !empty($book["title"]) &&
            isset($book["content"]) &&
            !empty($book["content"])

        ) {
        }
    }
}
